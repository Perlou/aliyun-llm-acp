# 📝 阿里云大模型 ACP 全真模拟试卷（四）

> ⏱️ 考试时长：120 分钟
>
> 📊 题目总数：100 道 (单选 70 + 多选 30)
>
> ✅ 合格分数：80 分

---

## 📋 考试说明

- 每题 1 分，满分 100 分
- 单选题选错不得分
- 多选题少选得 0.5 分，多选或错选不得分
- 建议时间分配：单选 60 分钟，多选 50 分钟，检查 10 分钟

---

## 第一部分：单选题 (1-70)

### 1. 提示词中提供"示例 (Examples)"的技术被称为：

A. Zero-shot Prompting  
B. Few-shot Prompting  
C. Chain of Thought  
D. ReAct

---

### 2. RAG 系统中，向量数据库存储的核心内容是：

A. 原始文档文本  
B. 文档的分类标签  
C. 文本切片的向量表示  
D. 用户查询记录

---

### 3. 当 top_p=0.9 时，模型的采样策略是：

A. 只选概率最高的 token  
B. 从累积概率达到 90% 的候选 token 集合中采样  
C. 随机选择任意 token  
D. 固定选择第 9 个 token

---

### 4. 以下哪项不是 LoRA 微调需要设置的参数？

A. r (秩)  
B. lora_alpha  
C. chunk_size  
D. target_modules

---

### 5. 在多智能体系统中，"数据分析"场景通常采用哪种协作模式？

A. 串行模式  
B. 并行模式（多角度分析后汇总）  
C. 层级模式  
D. 单 Agent 模式

---

### 6. 内积 (Inner Product) 在向量检索中的计算方式是：

A. 两向量对应元素差的平方和的平方根  
B. 两向量方向夹角的余弦值  
C. 两向量对应元素的乘积之和  
D. 两向量维度的差值

---

### 7. ActionTrail 在大模型安全场景中的典型作用是：

A. 密钥管理  
B. 身份认证  
C. 合规审计追踪，记录操作日志  
D. 内容过滤

---

### 8. 在 System Prompt 中设定"不回答涉及具体药物剂量的问题"属于：

A. 角色定义  
B. 输出格式要求  
C. 安全边界约束  
D. 背景信息补充

---

### 9. chunk_overlap=50 意味着相邻切片之间有多少 token 的重叠：

A. 50%  
B. 50 个 token  
C. 5 个 token  
D. 不重叠

---

### 10. 以下哪个是微调相比 RAG 的独特优势？

A. 知识实时更新  
B. 低成本部署  
C. 能从根本上改变模型的输出行为和风格  
D. 可追溯答案来源

---

### 11. LangChain 中 `ConversationBufferWindowMemory` 的工作方式是：

A. 保存所有历史  
B. 只保留最近 K 轮对话  
C. 对历史进行摘要  
D. 按关键词索引

---

### 12. 大模型对同一问题给出前后矛盾的回答，这最可能是：

A. 提示注入  
B. 偏见歧视  
C. 幻觉（不一致回答）  
D. 有害内容

---

### 13. LlamaIndex 中 `SimpleDirectoryReader` 的功能是：

A. 创建向量索引  
B. 从目录中批量加载文档  
C. 训练模型  
D. 部署服务

---

### 14. 以下哪个 HTTP 状态码通常表示 API Key 无效？

A. 200  
B. 401  
C. 404  
D. 500

---

### 15. Function Calling 中 `function.arguments` 返回的数据类型是：

A. Python 字典  
B. JSON 字符串（需 json.loads 解析）  
C. 列表  
D. 整数

---

### 16. Milvus 与 FAISS 的主要区别在于：

A. FAISS 不支持向量搜索  
B. Milvus 是分布式向量数据库，支持大规模生产部署  
C. 两者功能完全相同  
D. FAISS 只能用于文本

---

### 17. 微调建议的数据量中，专业领域任务通常需要：

A. 10~50 条  
B. 100~500 条  
C. 2000~10000 条  
D. 100 万条以上

---

### 18. DashScope 中 `model_name="qwen-max"` 指定的是：

A. 数据集名称  
B. 要使用的基础模型  
C. 微调任务名  
D. API 版本号

---

### 19. 句子窗口检索中 `window_size=3` 的效果是：

A. 只检索 3 个文档  
B. 检索到匹配句子后返回该句子前后各 3 个句子  
C. 向量维度为 3  
D. 分为 3 个窗口

---

### 20. Function Calling 中，当 `tool_calls` 包含多个工具调用时：

A. 只能执行第一个  
B. 需要逐个解析并执行每个工具调用  
C. 自动执行所有工具  
D. 返回错误

---

### 21. `gradient_accumulation_steps` 的作用是：

A. 增加显存占用  
B. 累积多个小 batch 的梯度再更新，等效于增大 batch_size  
C. 加速推理  
D. 减少训练轮次

---

### 22. 以下代码 `response.choices[0].message.content` 用于获取：

A. 流式输出的增量内容  
B. 非流式输出的完整回复文本  
C. 工具调用参数  
D. API 错误信息

---

### 23. Accuracy 指标最适合评估的任务是：

A. 文本摘要  
B. 机器翻译  
C. 文本分类  
D. 语言建模

---

### 24. 百炼 Assistant 中 `tools=[{"type": "retrieval"}]` 表示启用：

A. 工具调用  
B. 知识检索 (RAG)  
C. 代码执行  
D. 网页搜索

---

### 25. 代码生成任务建议使用的 temperature 值是：

A. 1.5  
B. 0.7  
C. 0  
D. 2.0

---

### 26. RAGAS 中 Faithfulness 值为 0.6 表示：

A. 60% 的检索内容被使用  
B. 答案中 60% 的陈述可由检索内容支撑  
C. 模型准确率为 60%  
D. 60% 的问题被正确回答

---

### 27. TTS 在语音助手中的作用是：

A. 语音识别  
B. 语音转文字  
C. 文字转语音（语音合成）  
D. 音频分析

---

### 28. 以下哪种方式是最不安全的 API Key 管理方式？

A. 环境变量  
B. 密钥管理服务 (KMS)  
C. 直接硬编码在源代码中  
D. 配置文件并加入 .gitignore

---

### 29. 微调中 `target_modules=["q_proj", "v_proj"]` 表示：

A. 对所有层进行微调  
B. 只对注意力机制的 Query 和 Value 投影层注入 LoRA 适配器  
C. 指定数据集路径  
D. 控制输出层

---

### 30. 以下哪种文档格式对 RAG 的解析挑战最大？

A. 纯文本 .txt  
B. Markdown .md  
C. 包含表格和扫描图的 PDF  
D. JSON 文件

---

### 31. 安全事件响应的第一步是：

A. 修复  
B. 复盘  
C. 发现（自动告警/用户举报）  
D. 遏制

---

### 32. LangChain 的 `Agents` 组件实现的核心功能是：

A. 数据存储  
B. 让 LLM 能够使用外部工具完成任务  
C. 模型训练  
D. 日志记录

---

### 33. 以下哪种方式可以将本地图片传递给 Qwen-VL？

A. 仅支持 URL  
B. 将图片 base64 编码后通过 `data:image/jpeg;base64,...` 格式传入  
C. 通过 FTP 上传  
D. 将图片转为文本描述

---

### 34. warmup_ratio=0.1 表示：

A. 模型预热 10 秒  
B. 训练前 10% 的步骤中学习率逐步增大  
C. 使用 10% 的数据  
D. 丢弃 10% 的参数

---

### 35. 微调数据中"代表性"的含义是：

A. 数据量越多越好  
B. 数据应反映真实场景的分布  
C. 数据格式统一  
D. 数据无重复

---

### 36. 以下哪个组件负责将用户 Query 转换为向量？

A. LLM  
B. Embedding 模型  
C. 重排序模型  
D. 向量数据库

---

### 37. 隐私合规的常见方案是：

A. 增加 temperature  
B. 数据脱敏  
C. 增大 batch_size  
D. 使用更多 epochs

---

### 38. LangChain 中 `ChatModels` 与 `LLMs` 的主要区别是：

A. ChatModels 只能生成图片  
B. ChatModels 接受消息格式输入，LLMs 接受文本格式输入  
C. LLMs 不能生成文本  
D. 两者完全相同

---

### 39. 当对话历史过长导致 API 报错"token limit exceeded"时，最佳做法是：

A. 切换更小的模型  
B. 删除 system prompt  
C. 对较早的对话历史进行截断或使用摘要压缩  
D. 重复发送请求

---

### 40. 客服系统中使用"路由 Agent → 专业客服 Agent"属于哪种模式？

A. 串行模式  
B. 并行模式  
C. 层级模式  
D. 随机分配

---

### 41. 通义万相生成图片时，输入的核心内容是：

A. 图片 URL  
B. 文本描述（Prompt）  
C. 音频文件  
D. 视频帧

---

### 42. 以下哪种情况说明需要增大 chunk_overlap？

A. 切片内容重复太多  
B. 切片边界处的信息经常丢失或断裂  
C. 检索速度太慢  
D. 存储空间不够

---

### 43. 以下关于"知情同意"的说法正确的是：

A. 只对政府机构适用  
B. 收集使用用户数据前需告知目的并获得用户授权  
C. 只适用于医疗场景  
D. 数据加密即可替代知情同意

---

### 44. 多因素认证 (MFA) 在云安全中属于：

A. 网络安全  
B. 身份认证  
C. 数据安全  
D. 审计追踪

---

### 45. 以下关于 Faithfulness 和 Answer Relevancy 的说法正确的是：

A. 两者评估相同维度  
B. Faithfulness 评估答案与 Context 的一致性，Answer Relevancy 评估答案与 Question 的相关性  
C. 两者都基于 Context 计算  
D. Answer Relevancy 不需要问题输入

---

### 46. 当 Function Calling 返回的 `finish_reason="tool_calls"` 时，表示：

A. 模型已给出最终回答  
B. 模型判断需要调用工具  
C. 请求失败  
D. Token 已用完

---

### 47. 如果训练 loss 一直不下降，最可能的原因是：

A. 学习率太大导致无法收敛  
B. 数据量太大  
C. batch_size 太大  
D. temperature 太高

---

### 48. 日志审计在安全防护中的作用是：

A. 过滤有害内容  
B. 对齐模型行为  
C. 记录操作行为，支持事后追溯和合规检查  
D. 检测提示注入

---

### 49. RAG 系统中 query_engine 的核心功能是：

A. 训练模型  
B. 接收用户查询→检索→LLM 生成→返回答案  
C. 管理数据库  
D. 部署模型

---

### 50. 以下哪个不是常见的语音识别 (ASR) 系统的输出？

A. 转录文字  
B. 时间戳对齐的文字  
C. 合成语音  
D. 说话人识别标签

---

### 51. 全参数微调相比 LoRA 的劣势是：

A. 效果差  
B. 需要大量 GPU 资源，成本极高  
C. 无法改变模型行为  
D. 只能用于小模型

---

### 52. LangChain 中 `input_variables` 在 PromptTemplate 中的作用是：

A. 定义输出格式  
B. 声明模板中需要动态填充的变量名  
C. 设置模型参数  
D. 指定数据库连接

---

### 53. Qwen-VL 处理图表数据时的能力被称为：

A. 语音识别  
B. 图表理解  
C. 代码生成  
D. 文本翻译

---

### 54. 以下哪种情况表明模型泛化能力好？

A. 训练 loss 很低但验证 loss 很高  
B. 训练 loss 和验证 loss 都较低且接近  
C. 训练 loss 不断下降而验证 loss 不断上升  
D. 验证 loss 远低于训练 loss

---

### 55. `index.as_query_engine(llm=llm)` 中传入 llm 参数的作用是：

A. 用于训练索引  
B. 指定用于生成回答的 LLM  
C. 创建 Embedding  
D. 管理缓存

---

### 56. 以下哪项不属于运营阶段安全清单？

A. 监控报警配置  
B. 定期安全审计  
C. 模型训练参数优化  
D. 应急响应预案

---

### 57. PAI-EAS 与 vLLM 的共同点是：

A. 都是训练框架  
B. 都可用于大模型推理部署  
C. 都是向量数据库  
D. 都是数据标注工具

---

### 58. Adapter 微调与 LoRA 的区别是：

A. 两者原理完全相同  
B. Adapter 插入独立适配层，LoRA 通过低秩分解修改现有层  
C. Adapter 参数量更少  
D. LoRA 不能用于 Transformer

---

### 59. BM25 算法考虑哪些因素进行检索？

A. 只考虑语义相似度  
B. 词频 (TF)、逆文档频率 (IDF) 和文档长度  
C. 向量维度  
D. 模型参数量

---

### 60. 安全事件响应中"根因分析"的目的是：

A. 临时封禁用户  
B. 定位问题的根本原因  
C. 发布新功能  
D. 增加 API 限额

---

### 61. 以下哪个模型最适合处理"识别图片中每个物体并标注位置"的任务？

A. Qwen-Max  
B. Qwen-Audio  
C. Qwen-VL  
D. Qwen-Plus

---

### 62. RAG 中 `rag_options.pipeline_id` 参数指定的是：

A. 模型 ID  
B. 百炼知识库的 Pipeline ID  
C. 用户 ID  
D. API Key

---

### 63. 以下哪个不是百炼平台支持的应用类型？

A. 智能问答  
B. 智能体  
C. 工作流  
D. 分布式模型训练

---

### 64. Context Recall 的计算需要哪项外部输入？

A. 只需要模型输出  
B. Ground Truth（标准答案）  
C. 只需要用户问题  
D. API Key

---

### 65. 以下哪个场景中 Agent 的规划 (Planning) 能力最为重要？

A. 单步翻译任务  
B. 需要多步骤完成的复杂任务（如先搜索再计算再汇总）  
C. 简单情感分析  
D. 格式化输出

---

### 66. 频率限制 (Rate Limiting) 在安全防护中属于：

A. 输入层  
B. 模型层  
C. 输出层  
D. 运营层

---

### 67. 以下哪种部署方式不适合大模型的生产环境？

A. PAI-EAS  
B. vLLM  
C. 直接在 Jupyter Notebook 中运行  
D. 函数计算（轻量场景）

---

### 68. LoRA 中 `lora_alpha=32` 与 `r=8` 的比例（alpha/r=4）表示：

A. 学习率为 4  
B. 适配器权重被放大 4 倍后与原始权重叠加  
C. 训练 4 个 epoch  
D. 使用 4 个 GPU

---

### 69. 备份恢复在云安全中属于：

A. 身份认证  
B. 网络安全  
C. 数据安全  
D. 审计追踪

---

### 70. 以下关于 CoT 和 ReAct 的对比，正确的是：

A. CoT 可以调用外部工具，ReAct 不能  
B. ReAct 是 CoT 的扩展，增加了行动和观察步骤  
C. 两者完全相同  
D. CoT 只能用于代码生成

---

## 第二部分：多选题 (71-100)

### 71. 分隔符在提示词工程中的作用包括：（多选）

A. 清晰区分指令和内容  
B. 防止提示词注入攻击  
C. 提高模型对结构的理解  
D. 加快推理速度

---

### 72. RAG 系统中向量化步骤需要的组件包括：（多选）

A. Embedding 模型  
B. 文档切片器  
C. 向量数据库  
D. 微调框架

---

### 73. 以下属于 LoRA 可注入的 target_modules 的有：（多选）

A. q_proj  
B. v_proj  
C. k_proj  
D. embedding_layer

---

### 74. 提示注入攻击的防范策略包括：（多选）

A. 使用分隔符隔离用户输入  
B. 部署关键词检测系统  
C. 在 system prompt 中声明忽略恶意指令  
D. 使用安全分类模型判断输入是否有害

---

### 75. RAG 召回优化技术包括：（多选）

A. 句子窗口检索  
B. 自动合并检索  
C. 混合检索  
D. 重排序

---

### 76. 多轮对话管理中需要维护的消息类型包括：（多选）

A. system 消息  
B. user 消息  
C. assistant 消息  
D. error 消息

---

### 77. Faithfulness 值较低可能的原因有：（多选）

A. 模型产生了幻觉  
B. 答案包含检索内容未提及的信息  
C. 检索结果与问题高度相关  
D. 模型忽略了检索到的上下文

---

### 78. 向量数据库的典型功能包括：（多选）

A. 高维向量存储  
B. 近似最近邻搜索  
C. 模型训练  
D. 索引管理

---

### 79. 模型层安全措施包括：（多选）

A. 安全对齐训练  
B. 拒绝机制  
C. 内容边界设定  
D. 敏感词过滤

---

### 80. 百炼平台微调后模型部署方式包括：（多选）

A. PAI-EAS  
B. vLLM  
C. 直接邮件发送模型文件  
D. 函数计算

---

### 81. 学习率 (learning_rate) 调优需要考虑的因素包括：（多选）

A. 过大会导致训练不稳定  
B. 过小会导致收敛慢  
C. 需要配合 warmup 使用  
D. 学习率越高效果一定越好

---

### 82. 以下涉及 tool role 消息的描述正确的有：（多选）

A. 用于将工具执行结果返回给模型  
B. 必须包含 tool_call_id 以对应具体的工具调用  
C. 内容为工具的执行结果  
D. 由模型自动生成

---

### 83. 多模态内容生产的流程步骤包括：（多选）

A. 用户需求分析  
B. 文案生成  
C. 图像生成  
D. 内容审核

---

### 84. 以下关于 max_tokens 的说法正确的有：（多选）

A. 控制模型输出的最大 token 数量  
B. 不影响输入 token 的计费  
C. 设置过小可能导致输出被截断  
D. 等同于 temperature 参数

---

### 85. 微调后评估时需要关注的方面包括：（多选）

A. 测试集上的指标（Loss、BLEU 等）  
B. 人工抽样评估output质量  
C. 对比微调前后的差异  
D. 只看训练 loss 即可

---

### 86. 数据传输安全的要求包括：（多选）

A. 加密传输 (HTTPS/TLS)  
B. 安全通道  
C. 明文传输即可  
D. 传输过程完整性校验

---

### 87. Agent 中 Memory（记忆）组件的作用包括：（多选）

A. 存储对话历史  
B. 提供上下文连续性  
C. 训练模型参数  
D. 支持多轮交互中的状态维护

---

### 88. 以下哪些因素会影响微调效果？（多选）

A. 数据质量  
B. 学习率设置  
C. 训练轮次  
D. API Key 的类型

---

### 89. 以下关于流式和非流式输出的说法正确的有：（多选）

A. 流式适合实时交互场景  
B. 非流式适合后台批量处理  
C. 流式通过 delta.content 获取增量内容  
D. 非流式通过 delta.content 获取内容

---

### 90. 安全事件响应流程的步骤包括：（多选）

A. 发现  
B. 评估  
C. 遏制与修复  
D. 复盘总结

---

### 91. RAG 系统性能优化可以从哪些维度入手？（多选）

A. 优化切片策略  
B. 使用更好的 Embedding 模型  
C. 引入重排序  
D. 增加模型参数量

---

### 92. 以下关于 RAM 权限管理的说法正确的有：（多选）

A. 可以创建子账号  
B. 支持细粒度权限策略  
C. 可控制 API 访问权限  
D. 只用于文件存储

---

### 93. QLoRA 的技术特点包括：（多选）

A. 使用 4-bit 量化降低显存  
B. 基于 LoRA 的低秩分解  
C. 效果与全参数微调完全一致  
D. 适合 GPU 资源有限的场景

---

### 94. RAG 文档解析面临的挑战包括：（多选）

A. PDF 表格提取  
B. 扫描件 OCR  
C. 多模态内容  
D. 格式保持

---

### 95. 以下哪些是使用 Agent 而非纯 LLM 的理由？（多选）

A. 需要调用外部 API 获取实时数据  
B. 需要执行多步骤复杂任务  
C. 需要与外部系统交互  
D. 只需简单文本生成

---

### 96. Assistant API 中 Run 的状态管理特点包括：（多选）

A. 异步执行  
B. 需要轮询检查状态  
C. 同步立即返回结果  
D. 可能包含多步工具调用

---

### 97. 输出层安全审核应覆盖的内容包括：（多选）

A. 有害和违法内容  
B. 事实准确性  
C. 敏感个人信息  
D. 模型训练日志

---

### 98. 微调流程中数据准备阶段的关键活动包括：（多选）

A. 收集和清洗数据  
B. 统一数据格式 (如 JSONL)  
C. 标注和审核数据质量  
D. 配置 GPU 集群

---

### 99. 重排序模型的作用包括：（多选）

A. 提高检索结果的精确相关性  
B. 弥补向量检索的语义偏差  
C. 对初步检索结果进行精细排序  
D. 替代向量数据库

---

### 100. 以下关于 Prompt Engineering、RAG 和微调的对比正确的有：（多选）

A. Prompt Engineering 不改变模型参数  
B. RAG 通过外部知识补充增强模型  
C. 微调改变模型权重以适应特定任务  
D. 三者不可以组合使用

---

## 📝 答案速查

<details>
<summary>点击查看全部答案</summary>

**单选题答案 (1-70)**

```
1.B   2.C   3.B   4.C   5.B   6.C   7.C   8.C   9.B   10.C
11.B  12.C  13.B  14.B  15.B  16.B  17.C  18.B  19.B  20.B
21.B  22.B  23.C  24.B  25.C  26.B  27.C  28.C  29.B  30.C
31.C  32.B  33.B  34.B  35.B  36.B  37.B  38.B  39.C  40.C
41.B  42.B  43.B  44.B  45.B  46.B  47.A  48.C  49.B  50.C
51.B  52.B  53.B  54.B  55.B  56.C  57.B  58.B  59.B  60.B
61.C  62.B  63.D  64.B  65.B  66.A  67.C  68.B  69.C  70.B
```

**多选题答案 (71-100)**

```
71.ABC   72.ABC   73.ABC   74.ABCD  75.ABCD
76.ABC   77.ABD   78.ABD   79.ABC   80.ABD
81.ABC   82.ABC   83.ABCD  84.AC    85.ABC
86.ABD   87.ABD   88.ABC   89.ABC   90.ABCD
91.ABC   92.ABC   93.ABD   94.ABCD  95.ABC
96.ABD   97.ABC   98.ABC   99.ABC   100.ABC
```

</details>

---

## 📊 自我评估

| 得分区间 | 评价     | 建议             |
| -------- | -------- | ---------------- |
| 90-100   | 优秀     | 可以考试了！     |
| 80-89    | 良好     | 再巩固薄弱点     |
| 70-79    | 中等     | 重点复习错题     |
| 60-69    | 及格边缘 | 需要系统复习     |
| <60      | 需要加强 | 回顾笔记重新学习 |

---

我的得分：**\_\_\_** 分

错题整理：

-
-
-
